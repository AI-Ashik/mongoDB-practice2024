<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
    <style>
      body {
        background-color: #1a202c;
        color: #e2e8f0;
      }
    </style>
  </head>
  <body>
    <header class="bg-gray-800 py-2">
      <nav class="container mx-auto flex justify-center">
        <a href="/read" class="text-white font-medium hover:underline">
          View Users
        </a>
      </nav>
    </header>
    <div class="flex justify-center items-center h-screen">
      <div class="w-full max-w-md">
        <form
          action="/create"
          method="post"
          class="bg-gray-800 p-8 rounded-lg shadow-lg"
        >
          <h2 class="text-2xl font-bold mb-6 text-center">Create User</h2>

          <div class="mb-4">
            <label class="block font-medium mb-2" for="name">Name</label>
            <input
              class="bg-gray-700 border-gray-600 rounded-md py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-500"
              type="text"
              id="name"
              name="name"
              placeholder="Enter your name"
              required
            />
          </div>

          <div class="mb-4">
            <label class="block font-medium mb-2" for="email">Email</label>
            <input
              class="bg-gray-700 border-gray-600 rounded-md py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-500"
              type="email"
              id="email"
              name="email"
              placeholder="Enter your email"
              required
            />
          </div>

          <div class="mb-4">
            <label class="block font-medium mb-2" for="image">Image URL</label>
            <input
              class="bg-gray-700 border-gray-600 rounded-md py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-500"
              type="url"
              id="image"
              name="image"
              placeholder="Enter image URL"
              required
            />
          </div>

          <button
            class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-md focus:outline-none focus:ring focus:border-blue-500"
            type="submit"
          >
            Register
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
